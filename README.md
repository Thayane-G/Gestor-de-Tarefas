# Task-Manager
Sistema de gerenciamento de tarefas desenvolvido em Java com integração ao MySQL utilizando JDBC. O projeto implementa operações CRUD, organização em camadas (DAO, model e connection) e interação via terminal, permitindo cadastrar, listar, atualizar e remover tarefas.
